# etc/logrotate.d/hitsu-phpmyadmin
/var/log/nginx/hitsu-phpmyadmin.*log {
    daily
    missingok
    rotate 14
    compress
    delaycompress
    notifempty
    create 0640 nginx nginx
    sharedscripts
    postrotate
        systemctl reload nginx > /dev/null 2>&1 || true
    endscript
}
